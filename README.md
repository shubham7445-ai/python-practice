# python-practice
Basic Python programs to practice loops, conditions, functions and file handling.  
Folders: -
`loops` – programs using `for` and `while` -
`conditions` – programs using `if-else` - 
`functions` – programs using user-defined functions - 
`file_handling` – read/write files in Python
